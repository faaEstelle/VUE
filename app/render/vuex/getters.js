import types from './mutation-types'

export default {
  // async ReadFile ({commit}, Path) {
  //   try {
  //     return await types.Read_File(Path)
  //   } catch (err) {
  //     commit(types.Add_To_Messages, {Message: err})
  //     // console.error(err)
  //   }
  // }
}
